# 05 — 容器与应用服务

> 本目录存放各应用服务的详细配置文档。每新增一个服务后，在对应子目录下创建 `说明.md`。
> **前置依赖**：必须先完成 [04-系统扩展与容器层构建](../04-系统扩展与容器层构建.md) 中的所有步骤（OMV-Extras、Docker、GPU Toolkit均已就绪）。

---

## 目录结构

```text
05-容器与应用服务/
├── 说明.md              ← 本文件（目录总览）
└── [ServiceName]/       ← 各应用服务
    └── 说明.md          ← 该服务的详细配置及使用手册
```

## 服务清单

*每新增并验证一个服务后，在此追加一行。使用 `/new-service` 工作流完成部署。*

| 服务 | 分类 | 端口 | 配置文件 | 说明文档 |
| :--- | :--- | :--- | :--- | :--- |
| — | — | — | — | 暂无 |

---

[返回总索引](../../README.md) | 上一步：[04-系统扩展与容器层构建](../04-系统扩展与容器层构建.md)
